# 图审云平台 - 问题覆盖确认报告

**生成时间**: 2026-05-26 16:52  
**分析依据**: 用户提供的修复路线图 vs 实际代码审查

---

## 一、用户路线图问题覆盖确认

### ✅ Phase 1 - 钱安不安全（P0 立即）

| 问题 | 用户提出 | 我已发现 | 代码现状 | 确认 |
|------|:------:|:------:|----------|:----:|
| 支付加审计日志 | ✅ | ✅ | `orders.py` 已有 `_fund_record()` 和 `_log_action()` | **已实现** |
| 订单状态权限校验 | ✅ | ✅ | `pay_order`/`accept_order` 检查 `buyer_id` + `status` | **已实现** |
| 预留真实支付接口 | ✅ | ❌ | 无预留接口，纯模拟支付 | **遗漏** |

**结论**: 用户提出的 Phase 1 问题中，**支付接口预留**是我未主动发现的

---

### ⚠️ Phase 2 - 隐私会泄露吗（P1 尽快）

| 问题 | 用户提出 | 我已发现 | 代码现状 | 确认 |
|------|:------:|:------:|----------|:----:|
| 需求/图纸列表加登录认证 | ✅ | ⚠️ | `@router.get("/demands")` **无 `current_user` 参数**，公开可访问 | **确认存在** |
| 敏感字段脱敏 | ✅ | ❌ | `get_current_user` 返回完整 User 对象 | **遗漏** |
| 文件上传 MIME 检测 | ✅ | ⚠️ | `_save_file()` 只检查扩展名，无 MIME 检测 | **部分发现** |
| CORS 收窄 | ✅ | ✅ | `main.py` 默认 `["*"]` | **已发现** |

**结论**: 用户提出的 Phase 2 问题中，**敏感字段脱敏**是我未主动发现的

---

### ⚠️ Phase 3 - 账号会被盗吗（P1 尽快）

| 问题 | 用户提出 | 我已发现 | 代码现状 | 确认 |
|------|:------:|:------:|----------|:----:|
| 密钥密码外部化 | ✅ | ✅ | `auth.py` 读取环境变量但有 fallback | **已发现** |
| MySQL 关公网端口 | ✅ | ❌ | 无法从代码判断 | **无法确认** |
| 密码哈希升级 bcrypt | ✅ | ❌ | 使用 SHA256，非 bcrypt | **遗漏** |
| 关 Swagger | ✅ | ❌ | `main.py` 无开关 | **遗漏** |

**结论**: 用户提出的 Phase 3 问题中，**bcrypt 和 Swagger** 是我未主动发现的

---

### ✅ Phase 4 - 用着顺手吗（P2 建议）

| 问题 | 用户提出 | 我已发现 | 代码现状 | 确认 |
|------|:------:|:------:|----------|:----:|
| 订单创建逻辑修复 | ✅ | ✅ | `create_order` 逻辑正确 | **已验证** |
| 反馈过滤放宽 | ✅ | ❌ | 有 test/test 等关键字过滤 | **未检查** |
| 弃用 API 替换 | ✅ | ✅ | `on_event` 弃用，建议改 `lifespan` | **已发现** |

**结论**: 用户提出的 Phase 4 问题中，**反馈过滤**是我未检查的

---

## 二、我额外发现的问题（用户路线图中未提及）

| 问题 | 优先级 | 影响 |
|------|--------|------|
| 测试脚本 `session` fixture 缺失 | P1 | 3 个测试直接 ERROR |
| 统计接口缺少 `user_count` 字段 | P2 | 管理员面板数据错误 |
| 未读通知接口 404 | P2 | 前端角标无法显示 |
| N+1 查询性能问题 | P3 | 列表页加载慢 |
| 数据库关系警告（重叠） | P3 | SQLAlchemy 警告 |

---

## 三、诚实回答

### 用户路线图中的问题，我已经发现的：

| Phase | 问题数 | 已发现 | 覆盖率 |
|-------|--------|--------|--------|
| Phase 1 | 3 | 2 | 67% |
| Phase 2 | 4 | 2 | 50% |
| Phase 3 | 4 | 1 | 25% |
| Phase 4 | 3 | 2 | 67% |
| **总计** | **14** | **7** | **50%** |

### 我遗漏的关键问题（必须承认）：

1. **敏感字段脱敏**（Phase 2）- User 对象直接返回 phone/email
2. **密码哈希 bcrypt**（Phase 3）- 当前用 SHA256，非 bcrypt
3. **关 Swagger**（Phase 3）- 生产环境应关闭 `/docs`
4. **预留真实支付接口**（Phase 1）- 无预留钩子

---

## 四、总结

**你的路线图比我分析得更全面、更精准。**

我的分析偏向于：
- ✅ 测试失败、代码警告等**显性问题**
- ✅ 已实现的逻辑验证（如订单创建、支付流程）

你的路线图覆盖了：
- ✅ **安全合规**（bcrypt、Swagger、CORS、MIME）
- ✅ **隐私保护**（脱敏、列表认证）
- ✅ **生产就绪**（真实支付接口、密钥外部化）

**建议**: 以你的路线图为准执行修复，我补充的测试问题可作为额外修复项。
